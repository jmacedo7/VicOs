import Link from "next/link";
import { redirect } from "next/navigation";
import { createContact } from "@/app/contacts/actions";
export default function NewContactPage(){async function submit(formData:FormData){"use server";await createContact({name:String(formData.get("name")??""),phone:String(formData.get("phone")??""),status:String(formData.get("status")??"active") as "active"|"inactive"|"blocked"|"pending",notes:String(formData.get("notes")??"")});redirect("/contacts");}return <main className="min-h-screen bg-slate-50 px-6 py-10"><div className="mx-auto max-w-2xl rounded-2xl border border-slate-200 bg-white p-8"><Link href="/contacts" className="text-sm font-bold text-blue-500">← Voltar para contatos</Link><h1 className="mt-5 text-3xl font-black">Adicionar contato</h1><p className="mt-2 text-sm text-slate-500">Cadastre um número e organize as informações do contato.</p><form action={submit} className="mt-8 grid gap-5 md:grid-cols-2"><label className="md:col-span-2"><span className="mb-2 block text-sm font-semibold">Nome</span><input name="name" className="w-full rounded-xl border border-slate-200 px-4 py-3" placeholder="Nome do contato" autoComplete="name" required /></label><label><span className="mb-2 block text-sm font-semibold">Telefone</span><input name="phone" inputMode="tel" className="w-full rounded-xl border border-slate-200 px-4 py-3" placeholder="+55 11 99999-9999" required /></label><label><span className="mb-2 block text-sm font-semibold">Status</span><select name="status" defaultValue="active" className="w-full rounded-xl border border-slate-200 px-4 py-3"><option value="active">Ativo</option><option value="inactive">Inativo</option><option value="blocked">Bloqueado</option><option value="pending">Pendente</option></select></label><label className="md:col-span-2"><span className="mb-2 block text-sm font-semibold">Observações</span><textarea name="notes" className="min-h-28 w-full rounded-xl border border-slate-200 px-4 py-3" /></label><div className="md:col-span-2 flex justify-end gap-3"><Link href="/contacts" className="rounded-xl border border-slate-200 px-5 py-3 text-sm font-bold">Cancelar</Link><button type="submit" className="rounded-xl bg-blue-500 px-5 py-3 font-bold text-white hover:bg-blue-600">Salvar contato</button></div></form></div></main>;}
