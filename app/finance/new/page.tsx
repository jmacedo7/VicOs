import Link from "next/link";
import { redirect } from "next/navigation";
import { createExpense, createIncome } from "@/app/finance/actions";
export default function NewFinancePage(){async function submit(formData:FormData){"use server";const kind=String(formData.get("kind")??"income");const amount=Number(String(formData.get("amount")??"").replace(",","."));const description=String(formData.get("description")??"");const category=String(formData.get("category")??"");const date=String(formData.get("date")??"");if(kind==="expense")await createExpense({description,amount,category,dueDate:date||undefined});else await createIncome({description,amount,category,date:date||undefined});redirect("/finance");}return <main className="min-h-screen bg-slate-50 px-6 py-10"><div className="mx-auto max-w-2xl rounded-2xl border border-slate-200 bg-white p-8"><Link href="/finance" className="text-sm font-bold text-blue-500">← Voltar para financeiro</Link><h1 className="mt-5 text-3xl font-black">Nova movimentação</h1><p className="mt-2 text-sm text-slate-500">Registre uma receita ou despesa para manter o resultado atualizado.</p><form action={submit} className="mt-8 grid gap-5 md:grid-cols-2"><label><span className="mb-2 block text-sm font-semibold">Tipo</span><select name="kind" defaultValue="income" className="w-full rounded-xl border border-slate-200 px-4 py-3"><option value="income">Receita</option><option value="expense">Despesa</option></select></label><label><span className="mb-2 block text-sm font-semibold">Valor</span><input name="amount" type="number" min="0.01" step="0.01" className="w-full rounded-xl border border-slate-200 px-4 py-3" required /></label><label className="md:col-span-2"><span className="mb-2 block text-sm font-semibold">Descrição</span><input name="description" className="w-full rounded-xl border border-slate-200 px-4 py-3" placeholder="Ex.: Venda de serviço" required /></label><label><span className="mb-2 block text-sm font-semibold">Categoria</span><input name="category" className="w-full rounded-xl border border-slate-200 px-4 py-3" placeholder="Ex.: Vendas" /></label><label><span className="mb-2 block text-sm font-semibold">Data</span><input name="date" type="date" className="w-full rounded-xl border border-slate-200 px-4 py-3" /></label><div className="md:col-span-2 flex justify-end gap-3"><Link href="/finance" className="rounded-xl border border-slate-200 px-5 py-3 font-bold">Cancelar</Link><button type="submit" className="rounded-xl bg-blue-500 px-5 py-3 font-bold text-white hover:bg-blue-600">Salvar movimentação</button></div></form></div></main>;}
